name: pretty_json
description: Visualize JSON response in pretty format.
version: 1.0.0
homepage: https://github.com/Manty-K/pretty_json

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
flutter:
